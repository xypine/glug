[package]
name = "glug-glug-core"
version = "0.1.0"
edition = "2024"

[dependencies]
chrono = "0.4"
sqlx = { version = "0.8", features = [ "runtime-tokio", "tls-rustls-aws-lc-rs", "sqlite", "chrono" ] }
